#include <stdlib.h>

int     *ft_range(int start, int end)
{
	int	i;
	int	range;
	int	*d;

	range = (end - start);
	if (range < 0)
		range *= -1;
	d = malloc(sizeof(int) * range + 1);
	if (start > end)
	{
		i = 0;
		while ()
	}
}
